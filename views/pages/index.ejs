<!DOCTYPE html>

    <%- include('../partials/head') %> 

    <body class="container">

        <%- include('../partials/header', {descripage: 'Home'}) %> 

        <main>
            <div class="jumbotron">
                <h1>Node + EJS</h1>
                <p>Conceitos básicos de EJS</p>
            </div>
        </main>

        <ul>
            <% list.forEach(function(item) { %>
                
                <li>
                    <strong><%= item.Title %></strong> <%= item.Text %>
                </li>
                
            <% });  %> 
        </ul>

        <%- include('../partials/footer') %> 

    </body>
</html>